

# Empirical Stability of Least Squares and Ridge Regression

This project studies the empirical stability of **Least Squares** and **Ridge Regression**. The main goal is to understand how much model predictions change when one observation is removed from the training set, and how regularization affects the trade-off between stability and prediction accuracy.

The analysis includes synthetic datasets with independent and correlated predictors, an experiment on the effect of sample size, and the real **Seoul Bike Sharing Demand** dataset.

## Project Structure

```text
ridge-stability-project/
├── data/               # Seoul Bike Sharing dataset
├── figures/            # Figures generated by the experiments
├── report/             # LaTeX source and final PDF report
├── scripts/            # Functions used in the analysis
│   ├── datasets.py
│   ├── eda.py
│   ├── metrics.py
│   ├── models.py
│   ├── plots.py
│   └── stability.py
├── main.py             # Runs the complete analysis
├── requirements.txt    # Python dependencies
└── README.md
```

## Methods

Both regression methods are implemented from scratch using NumPy.

Model stability is evaluated with a **leave-one-out procedure**. The model is first fitted on the complete training set. Then, one training observation at a time is removed, the model is fitted again, and its predictions are compared on the same fixed test set. The average absolute change in predictions is used as the empirical stability measure: lower values indicate greater stability.

Predictive performance is evaluated using **Mean Squared Error (MSE)** on the training and test sets. The generalization gap is also computed as the difference between test and training MSE.

## Experiments

The project includes three main experiments:

1. **Synthetic data:** comparison of Least Squares and Ridge using independent predictors (BASE scenario) and strongly correlated predictors (CORRELATED scenario, $\rho = 0.8$).
2. **Sample size:** analysis of stability for $n \in \{50, 100, 200, 500\}$.
3. **Real data:** application to the Seoul Bike Sharing Demand dataset, using different values of the Ridge regularization parameter $\lambda$.

For Ridge, the experiments use:

```text
lambda = [0.001, 0.01, 0.1, 1, 10, 100]
```

## Dataset

The real-world experiment uses the **Seoul Bike Sharing Demand** dataset from the UCI Machine Learning Repository. The original dataset contains 8760 hourly observations and 14 variables describing bike rentals, weather conditions, and time information.

Observations corresponding to non-functioning days are removed before modeling. For the stability experiment, a sample of 1000 observations is used to keep the repeated leave-one-out computations manageable.

Dataset source: UCI Machine Learning Repository, *Seoul Bike Sharing Demand* (2020), DOI: 10.24432/C5F62R.

## Main Results

The experiments show that the effect of Ridge depends on both the structure of the data and the strength of regularization.

- With independent synthetic predictors, small values of $\lambda$ produce results very similar to Least Squares.
- With correlated predictors, moderate regularization improves stability. In particular, $\lambda = 0.1$ reduces the average prediction change while keeping the increase in Test MSE limited.
- Both Least Squares and Ridge become more stable as the sample size increases.
- On the Seoul Bike Sharing dataset, $\lambda = 0.1$ provides the best observed trade-off between stability and predictive performance. Stronger regularization further reduces sensitivity to individual observations but substantially increases prediction error.

## How to Run

Install the required dependencies:

```bash
pip install -r requirements.txt
```

Then run the complete analysis from the project root:

```bash
python main.py
```

The required Python packages are NumPy, pandas, and Matplotlib.

## Report

The complete methodology, experiments, results, figures, and conclusions are available in [`report/report.pdf`](report/report.pdf).

## Reference

Bousquet, O. and Elisseeff, A. (2002). *Stability and Generalization*. Journal of Machine Learning Research, 2, 499–526.